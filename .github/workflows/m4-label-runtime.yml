name: M4 label runtime gate

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"
          components: rustfmt
      - name: Apply label runtime integration
        run: python .github/allodium-m4-label-runtime.py
      - name: Format generated integration
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
      - name: Test
        run: cargo test --workspace --locked
      - name: Validate Allodium state
        run: cargo run --locked -p allodium-cli -- validate .
      - name: Smoke combined projection plan
        run: |
          cargo run --locked -p allodium-cli -- github plan . github > /tmp/allodium-plan.toml
          grep -q 'canonical_id = "label-allodium"' /tmp/allodium-plan.toml
          grep -q 'action = "create_label"' /tmp/allodium-plan.toml
      - name: Publish product and self-clean
        run: |
          rm .github/allodium-m4-label-runtime.py
          rm .github/workflows/m4-label-runtime.yml
          git add crates/allodium-core/src/github_label.rs crates/allodium-github/src/label.rs crates/allodium-github/src/lib.rs crates/allodium-cli/src/main.rs .github
          git config user.name 'Allodium implementation gate'
          git config user.email 'actions@users.noreply.github.com'
          git commit -m 'feat(github): add label projection runtime'
          git push origin HEAD:main

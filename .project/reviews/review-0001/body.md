This is Allodium's first deliberate live canonical-review projection test.

The authoritative review is this ordinary filesystem directory on `main`. Its GitHub pull request exists only as a projection target for testing review identity, observation, reconciliation, close semantics, and provider-scoped conversation/review provenance.

The proposed branch changes only `docs/dogfood/review-projection.md`; it carries no product implementation work and should remain open until M2's PR social-ingress paths have been dogfooded.

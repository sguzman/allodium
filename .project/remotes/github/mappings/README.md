# GitHub mappings

This subtree contains reconstructible relationships between canonical Allodium IDs and GitHub IDs.

Mappings are ordinary files and may be inspected or edited manually, but they are not canonical project meaning. Deleting this subtree must not destroy the project; an adapter should be able to rediscover or recreate mappings where the provider permits it.

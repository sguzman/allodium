# GitHub incoming archive

Externally originated GitHub activity is preserved here with GitHub-scoped provenance.

This archive does not silently mutate canonical project state and is not automatically replayed onto other remotes. Promotion into canonical state is explicit.
